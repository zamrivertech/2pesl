PRECISE PLANNING ENGENHARIA & SERVIÇOS, LDA — STATIC WEBSITE

Open index.html in a browser or upload the entire folder to any static host (GitHub Pages, Netlify, cPanel, etc.).
No database, spreadsheet, build step, framework or configuration is required.
The year in the footer updates automatically.
Language switch: PT / EN.

Important note about extra images:
The website already includes images extracted from the supplied company portfolios and is fully functional as-is.
Web browsers cannot automatically discover arbitrary new filenames inside ./img/ on normal static hosting. New images can be stored in ./img/, but using them on the page would require referencing their filenames in index.html.
